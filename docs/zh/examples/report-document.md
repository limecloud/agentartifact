---
title: 报告文档
description: 结构化报告的 artifact flow 示例。
---

# 报告文档

研究 Agent 创建 `report_document`，添加 summary block 与 comparison table，按 section 连接 sources，创建 version 1，生成 preview，并导出 Markdown 与 PDF。Evidence 可以引用 `artifact_id`、`version_id` 与 `part_id`。
